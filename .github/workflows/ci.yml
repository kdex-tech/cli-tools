name: CI

on:
  push:
    branches-ignore:
    - "dependabot/**"
    paths:
    - "**"
    - "!docs/**"
    tags:
    - "v*"
  pull_request:
    paths:
    - "**"
    - "!docs/**"
  # Manual fallback: if the push/tag triggers don't dispatch (we hit this
  # silently with v0.3.13 — GitHub registered the PushEvent but didn't
  # fire the workflow), use `gh workflow run CI -R kdex-tech/cli-tools
  # --ref <tag-or-branch>` to force a build/publish from any ref.
  workflow_dispatch:

env:
  LC_ALL: en_US.UTF-8
  PUBLISH: ${{ (github.repository == 'kdex-tech/cli-tools') && startsWith(github.ref, 'refs/tags/') && (github.event_name != 'pull_request') }}

defaults:
  run:
    shell: bash

permissions:
  packages: write
  contents: read

jobs:
  ci:
    name: Build, Test, Publish
    runs-on: ubuntu-latest
    steps:
    - name: Clone the code
      uses: actions/checkout@v6

    - name: Publish Docker Image
      if: ${{ env.PUBLISH == 'true' }}
      uses: ./.github/actions/publish-docker-image
